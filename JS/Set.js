(function(global, undefined) {
    
    var Set;

    Set.VERSION = '0.0.0';
    
    var _ = global._;
    if (!_ && (typeof require !== 'undefined')) {
        _ = require("underscore")._;
    }
    
    _.extend(Set, {
       "Create": function Create() {
           
       }
    });
    
}(this))